---
phase: 01.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/src/router/index.ts
  - web/src/components/SidebarNav.vue
  - web/src/locales/pt-BR.json
  - web/src/locales/en-US.json
  - web/src/api/traffic.ts
  - web/src/views/TrafficView.vue
autonomous: true
requirements: [D-11, D-12, D-13, D-14, D-20]
must_haves:
  truths:
    - "Frontend has uPlot as a runtime dependency (pinned in package.json + pnpm-lock.yaml)"
    - "Navigating to /traffic resolves to a TrafficView placeholder that renders inside AppLayout"
    - "Sidebar shows a 'Tráfego' / 'Traffic' entry that links to /traffic and highlights when active"
    - "pt-BR and en-US locales both contain a complete `traffic.*` key set used by every label rendered in this phase"
    - "A typed `traffic.ts` API client wraps the two REST endpoints introduced by Plan 03"
  artifacts:
    - path: web/package.json
      provides: "uplot dependency"
      contains: "uplot"
    - path: web/src/router/index.ts
      provides: "/traffic route entry"
      contains: "path: '/traffic'"
    - path: web/src/components/SidebarNav.vue
      provides: "traffic menu item"
      contains: "nav.traffic"
    - path: web/src/locales/pt-BR.json
      provides: "traffic.* keys (pt-BR)"
      contains: "traffic.title"
    - path: web/src/locales/en-US.json
      provides: "traffic.* keys (en-US)"
      contains: "traffic.title"
    - path: web/src/api/traffic.ts
      provides: "typed client wrappers for /api/traffic/top20 and /api/traffic/host/{ip}"
      contains: "export async function fetchTop20"
    - path: web/src/views/TrafficView.vue
      provides: "skeleton view registered at /traffic"
      contains: "AppLayout"
  key_links:
    - from: web/src/router/index.ts
      to: web/src/views/TrafficView.vue
      via: "import + route registration"
      pattern: "TrafficView"
    - from: web/src/components/SidebarNav.vue
      to: web/src/router/index.ts
      via: "routeMap['traffic'] = '/traffic'"
      pattern: "traffic.*'/traffic'"
    - from: web/src/api/traffic.ts
      to: web/src/api/client.ts
      via: "uses the shared api() wrapper that handles CSRF + cookies"
      pattern: "from '@/api/client'"
---

<objective>
Lay the frontend foundation for the new `/traffic` view independent of the backend handlers and the chart component. After this plan:

- uPlot is installed (D-11).
- The route, sidebar entry, locale keys, and a typed API client all exist (D-13, D-14, D-20).
- A minimal `TrafficView.vue` placeholder renders in `AppLayout` so the route resolves and the navigation works end-to-end even before Plan 03 (handlers) and Plan 05 (full UI) land.

This plan is intentionally backend-independent (it does NOT call the new endpoints yet — the typed client is just declared). That lets Wave 1 run Plan 01 and Plan 02 in parallel without file conflicts.

Purpose: every other FE plan (04 chart, 05 view) builds on these foundations. Keeping them in one plan avoids three Vue plans that each add one file.

Output: dependency added, route registered, sidebar updated, i18n keys committed in both locales, typed API client stubbed, placeholder view rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-CONTEXT.md

# FE patterns to mirror — read before editing
@web/src/router/index.ts
@web/src/components/SidebarNav.vue
@web/src/components/AppLayout.vue
@web/src/locales/pt-BR.json
@web/src/locales/en-US.json
@web/src/api/client.ts
@web/src/views/DashboardView.vue
@web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uPlot dependency and verify install</name>
  <files>web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/package.json (confirm pnpm; dependencies block has naive-ui, vue, etc.)
    - web/pnpm-lock.yaml (top — confirms pnpm lockfile is the source of truth)
    - .planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-CONTEXT.md §D-11 (uplot ~40 KB gz, ONLY chart lib introduced in this phase)
  </read_first>
  <action>
    Run, from `/home/david/workspace/mitigador/web/`:

    ```
    pnpm add uplot
    ```

    This will:
    - Add `"uplot": "^1.6.x"` (latest stable, currently 1.6.x in mid-2026) to `dependencies` in `web/package.json`.
    - Update `web/pnpm-lock.yaml`.

    Do NOT install other charting libs (no echarts, no chart.js, no recharts — D-11 mandates uPlot only). Do NOT install `@types/uplot` — uPlot ships its own `.d.ts` (verify by checking that `node_modules/uplot/dist/uPlot.d.ts` exists after install).

    If `pnpm add` fails because pnpm is not on PATH, the executor MUST install pnpm first (`corepack enable && corepack prepare pnpm@latest --activate` or `npm install -g pnpm`) — but the resulting `pnpm-lock.yaml` MUST be regenerated, not bypassed by using `npm install`. Mixing package managers is forbidden (would orphan `pnpm-lock.yaml`).
  </action>
  <verify>
    <automated>cd /home/david/workspace/mitigador/web &amp;&amp; grep -q '"uplot"' package.json &amp;&amp; grep -q "uplot" pnpm-lock.yaml &amp;&amp; test -f node_modules/uplot/dist/uPlot.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"uplot"' web/package.json` exits 0
    - `grep -q "uplot" web/pnpm-lock.yaml` exits 0
    - `web/node_modules/uplot/dist/uPlot.d.ts` exists (proves install + types ship in-package)
    - Lockfile is still pnpm format (file starts with `lockfileVersion:`) — no `package-lock.json` or `yarn.lock` added
    - `git diff web/package.json` shows exactly one new line under `dependencies` (the uplot entry); no removed or reordered deps
  </acceptance_criteria>
  <done>
    uPlot is in `dependencies` of `web/package.json`, lockfile updated, types resolvable from `node_modules/uplot/dist/uPlot.d.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add traffic.* i18n keys to pt-BR and en-US locales</name>
  <files>web/src/locales/pt-BR.json, web/src/locales/en-US.json</files>
  <read_first>
    - web/src/locales/pt-BR.json (full file — understand the flat-key convention `section.subsection.field`)
    - web/src/locales/en-US.json (full file — same keys must exist in both)
    - .planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-CONTEXT.md §D-13, D-20 ("Tráfego" / "Traffic"; all strings under `traffic.*`; no hardcoded text)
  </read_first>
  <action>
    Edit BOTH locale files. Add exactly these keys (insert them alphabetically — between `sse.*` and `vetor.*` works, or wherever fits; existing keys are roughly grouped but not strictly sorted, so prefer grouping all `traffic.*` together near the other view groups for readability).

    Add to `web/src/locales/pt-BR.json`:

    ```json
    "nav.traffic": "Tráfego",
    "traffic.title": "Tráfego em Tempo Real",
    "traffic.subtitle": "Top 20 hosts mais ruidosos nos últimos 60s",
    "traffic.col.ip": "IP",
    "traffic.col.hostgroup": "Hostgroup",
    "traffic.col.bps": "BPS",
    "traffic.col.pps": "PPS",
    "traffic.col.proto": "Protocolo Dominante",
    "traffic.proto.udp": "UDP",
    "traffic.proto.icmp": "ICMP",
    "traffic.proto.other": "Outros",
    "traffic.hostgroup.none": "—",
    "traffic.empty.title": "Nenhum host com tráfego",
    "traffic.empty.body": "Nada chegando nos últimos 60s. Verifique se há exporters online.",
    "traffic.error.fetch": "Falha ao carregar tráfego. Tentando novamente…",
    "traffic.error.stale": "Dados podem estar desatualizados.",
    "traffic.toggle.bps": "BPS",
    "traffic.toggle.pps": "PPS",
    "traffic.drawer.title": "Tráfego de {ip}",
    "traffic.drawer.close": "Fechar",
    "traffic.chart.series.udp": "UDP",
    "traffic.chart.series.icmp": "ICMP",
    "traffic.chart.series.other": "Outros",
    "traffic.chart.axis.time": "Tempo (s atrás)",
    "traffic.chart.axis.bps": "bps",
    "traffic.chart.axis.pps": "pps",
    "traffic.chart.tooltip.now": "agora",
    "traffic.chart.tooltip.seconds_ago": "há {n}s",
    "traffic.chart.empty": "Sem dados para este host.",
    "traffic.host.not_found": "Host não encontrado nos últimos 60s."
    ```

    Add to `web/src/locales/en-US.json` (same keys, English values):

    ```json
    "nav.traffic": "Traffic",
    "traffic.title": "Real-Time Traffic",
    "traffic.subtitle": "Top 20 noisiest hosts in the last 60s",
    "traffic.col.ip": "IP",
    "traffic.col.hostgroup": "Hostgroup",
    "traffic.col.bps": "BPS",
    "traffic.col.pps": "PPS",
    "traffic.col.proto": "Dominant Protocol",
    "traffic.proto.udp": "UDP",
    "traffic.proto.icmp": "ICMP",
    "traffic.proto.other": "Other",
    "traffic.hostgroup.none": "—",
    "traffic.empty.title": "No host traffic",
    "traffic.empty.body": "Nothing received in the last 60s. Check that exporters are online.",
    "traffic.error.fetch": "Failed to load traffic. Retrying…",
    "traffic.error.stale": "Data may be stale.",
    "traffic.toggle.bps": "BPS",
    "traffic.toggle.pps": "PPS",
    "traffic.drawer.title": "Traffic for {ip}",
    "traffic.drawer.close": "Close",
    "traffic.chart.series.udp": "UDP",
    "traffic.chart.series.icmp": "ICMP",
    "traffic.chart.series.other": "Other",
    "traffic.chart.axis.time": "Time (s ago)",
    "traffic.chart.axis.bps": "bps",
    "traffic.chart.axis.pps": "pps",
    "traffic.chart.tooltip.now": "now",
    "traffic.chart.tooltip.seconds_ago": "{n}s ago",
    "traffic.chart.empty": "No data for this host.",
    "traffic.host.not_found": "Host not seen in the last 60s."
    ```

    JSON validity: trailing-comma errors break vite-tsc. Make sure each insertion preserves valid JSON. Run `node -e "JSON.parse(require('fs').readFileSync('web/src/locales/pt-BR.json'))"` (and the en-US equivalent) as a sanity check before committing.

    Do NOT delete or rename any existing keys. Do NOT change `nav.live`, `nav.exporters`, `nav.bgp`, `nav.incidents`, `nav.logout`.
  </action>
  <verify>
    <automated>cd /home/david/workspace/mitigador &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('web/src/locales/pt-BR.json'));JSON.parse(require('fs').readFileSync('web/src/locales/en-US.json'));console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"nav.traffic"' web/src/locales/pt-BR.json` exits 0
    - `grep -q '"nav.traffic"' web/src/locales/en-US.json` exits 0
    - `grep -c '"traffic\.' web/src/locales/pt-BR.json` returns ≥ 28
    - `grep -c '"traffic\.' web/src/locales/en-US.json` returns ≥ 28
    - Both files parse as valid JSON (the verify command above prints `ok`)
    - pt-BR contains "Tráfego" exactly once: `grep -c '"Tráfego"' web/src/locales/pt-BR.json` returns 1
    - en-US `nav.traffic` value is exactly `"Traffic"`: `grep -q '"nav.traffic": "Traffic"' web/src/locales/en-US.json` exits 0
    - No existing key was deleted: `grep -q '"nav.live"' web/src/locales/pt-BR.json` AND `grep -q '"ataques.titulo"' web/src/locales/pt-BR.json` BOTH exit 0
  </acceptance_criteria>
  <done>
    Both locale files contain a complete, parseable `traffic.*` key set plus `nav.traffic`. No existing keys removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register /traffic route and add sidebar menu entry</name>
  <files>web/src/router/index.ts, web/src/components/SidebarNav.vue, web/src/views/TrafficView.vue</files>
  <read_first>
    - web/src/router/index.ts (entire — pattern of `path/name/component`)
    - web/src/components/SidebarNav.vue (entire — `menuOptions` array + `routeMap`)
    - web/src/views/DashboardView.vue lines 1-50 (placeholder import/layout pattern — AppLayout slot usage)
    - .planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-CONTEXT.md §D-13, D-14
  </read_first>
  <action>
    Step A — Create `web/src/views/TrafficView.vue` as a minimal placeholder that ALREADY uses AppLayout and the i18n title. This is intentionally tiny — Plan 05 fleshes it out.

    ```vue
    <script setup lang="ts">
    import { useI18n } from 'vue-i18n';
    import { NEmpty } from 'naive-ui';
    import AppLayout from '@/components/AppLayout.vue';

    const { t } = useI18n();
    </script>

    <template>
      <AppLayout>
        <template #header-title>
          <h1 class="page-title">{{ t('traffic.title') }}</h1>
        </template>

        <!-- Placeholder body — fully replaced by plan 05. Keep AppLayout wiring so the route renders and i18n works. -->
        <NEmpty :description="t('traffic.empty.title')">
          <template #extra>
            <span class="empty-body">{{ t('traffic.empty.body') }}</span>
          </template>
        </NEmpty>
      </AppLayout>
    </template>

    <style scoped>
    .page-title {
      font-size: 20px;
      font-weight: 600;
      color: #e0e0e0;
      margin: 0;
    }
    .empty-body {
      font-size: 13px;
      color: #a0a0a0;
      max-width: 360px;
      text-align: center;
      display: block;
      margin-top: 8px;
    }
    </style>
    ```

    Step B — Edit `web/src/router/index.ts`:

    1. Add the import (alphabetical with existing imports):
       ```ts
       import TrafficView from '@/views/TrafficView.vue';
       ```
    2. Add the route entry inside the `routes` array, after the dashboard entry and before exporters (so the order stays "Live -> Traffic -> Exporters -> BGP -> Incidents" matching what the sidebar will show):
       ```ts
       { path: '/traffic', name: 'traffic', component: TrafficView },
       ```

    Step C — Edit `web/src/components/SidebarNav.vue`:

    1. Inside the `menuOptions` computed array, insert a new option right after `dashboard` and before `exporters`:
       ```ts
       {
         label: t('nav.traffic'),
         key: 'traffic',
       },
       ```
    2. Inside the `routeMap` object, add the mapping:
       ```ts
       traffic: '/traffic',
       ```
    3. Do NOT add a badge for traffic (no count to show; the dashboard's active-attack badge is the only badge in the sidebar).

    Do NOT change any other route, any other menu item, or the layout/styling of the sidebar.
  </action>
  <verify>
    <automated>cd /home/david/workspace/mitigador/web &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/src/views/TrafficView.vue`
    - `grep -q "AppLayout" web/src/views/TrafficView.vue` exits 0
    - `grep -q "traffic.title" web/src/views/TrafficView.vue` exits 0
    - `grep -q "import TrafficView" web/src/router/index.ts` exits 0
    - `grep -q "path: '/traffic'" web/src/router/index.ts` exits 0
    - `grep -q "name: 'traffic'" web/src/router/index.ts` exits 0
    - `grep -q "nav.traffic" web/src/components/SidebarNav.vue` exits 0
    - `grep -q "traffic: '/traffic'" web/src/components/SidebarNav.vue` exits 0
    - `pnpm --dir web typecheck` exits 0 (vue-tsc reports no errors — proves i18n keys resolve and Vue SFC is valid)
    - No existing route was deleted: `grep -c "name: '" web/src/router/index.ts` returns 6 (was 5, +1 for traffic; login + dashboard + traffic + exporters + bgp + incidents = 6; incident detail uses name:'incident' so already counted)
  </acceptance_criteria>
  <done>
    Navigating to `/traffic` resolves to `TrafficView` (a placeholder NEmpty rendered inside `AppLayout`); the sidebar shows "Tráfego" (or "Traffic") between Live and Exporters and the active-class highlights when the user is on `/traffic`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create web/src/api/traffic.ts typed client for the two new endpoints</name>
  <files>web/src/api/traffic.ts</files>
  <read_first>
    - web/src/api/client.ts (entire — understand the `api<T>(path, opts)` wrapper signature; new wrappers MUST go through it for CSRF/cookies)
    - .planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-CONTEXT.md §D-18 (endpoint paths) + §D-05 (top-20 fields) + §D-07/D-09 (per-host bucket fields)
  </read_first>
  <action>
    Create `web/src/api/traffic.ts`:

    ```ts
    // Typed client wrappers for the Plan 03 endpoints:
    //   GET /api/traffic/top20            → TopTalkersResponse
    //   GET /api/traffic/host/{ip}        → HostTrafficResponse  (404 if unknown)
    //
    // Both routes are authenticated and live inside the chi r.Group that already
    // does cookie/session + CSRF. Since both are GET, no CSRF header is needed.

    import { api } from '@/api/client';

    export type DominantProto = 'udp' | 'icmp' | 'other';

    export interface TopTalker {
      ip: string;
      hostgroup: string | null;   // null when no longest-prefix-match in the hostgroups table
      bps: number;
      pps: number;
      dominant_proto: DominantProto;
    }

    export interface TopTalkersResponse {
      items: TopTalker[];          // up to 20 entries, sorted bps desc
      generated_at: string;        // RFC3339 server timestamp
    }

    export interface HostBucket {
      // Offset in seconds from `generated_at`: 0 = now, 1 = 1s ago, ..., 59 = 59s ago.
      offset_s: number;
      pps: number;
      bps: number;
      pps_udp: number;
      bps_udp: number;
      pps_icmp: number;
      bps_icmp: number;
      pps_other: number;
      bps_other: number;
    }

    export interface HostTrafficResponse {
      ip: string;
      hostgroup: string | null;
      generated_at: string;
      // Newest-first: index 0 = "now" (matches aggregate.Store.Snapshot semantics).
      buckets: HostBucket[];
    }

    export function fetchTop20(): Promise<TopTalkersResponse> {
      return api<TopTalkersResponse>('/api/traffic/top20');
    }

    export function fetchHostTraffic(ip: string): Promise<HostTrafficResponse> {
      // Encode the IP since v6 addresses contain colons.
      return api<HostTrafficResponse>(`/api/traffic/host/${encodeURIComponent(ip)}`);
    }
    ```

    Notes for the executor:
    - Keep this file self-contained — only `api` is imported from `client.ts`.
    - Do NOT call these functions yet. Plan 05 wires them up. The placeholder `TrafficView` from Task 3 does not need them.
    - `dominant_proto` snake_case keys mirror the Go JSON tags Plan 03 will emit. If Plan 03 uses camelCase, Plan 05 will adjust here — but per the existing pattern in `internal/api/exporters.go` (which emits `source_ip`, `last_seen`, `flows_per_sec`), snake_case IS the established convention.
  </action>
  <verify>
    <automated>cd /home/david/workspace/mitigador/web &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/src/api/traffic.ts`
    - `grep -q "export async function fetchTop20\|export function fetchTop20" web/src/api/traffic.ts` exits 0
    - `grep -q "export async function fetchHostTraffic\|export function fetchHostTraffic" web/src/api/traffic.ts` exits 0
    - `grep -q "encodeURIComponent" web/src/api/traffic.ts` exits 0
    - `grep -q "from '@/api/client'" web/src/api/traffic.ts` exits 0
    - `pnpm --dir web typecheck` exits 0 (proves the TS types compile)
    - No direct `fetch(` call: `grep -q "fetch(" web/src/api/traffic.ts` returns nonzero (must go through `api()` so CSRF/cookies stay centralized)
  </acceptance_criteria>
  <done>
    `web/src/api/traffic.ts` exposes typed `fetchTop20` and `fetchHostTraffic` wrappers that route through the shared `api()` client. Types compile.
  </done>
</task>

<task type="auto">
  <name>Task 5: Frontend build smoke — pnpm build must produce a clean SPA bundle including the new route</name>
  <files>(none — verification-only task; outputs go to internal/api/web_dist which is gitignored)</files>
  <read_first>
    - web/vite.config.ts (confirms `outDir = ../internal/api/web_dist`, `emptyOutDir: true`)
    - web/.gitignore (confirms web_dist is not committed)
    - .planning/phases/01-observation-spine/01-11-SUMMARY.md (confirms the existing build pipeline so the executor knows what "passing" looks like)
  </read_first>
  <action>
    From `/home/david/workspace/mitigador/web/`, run:

    ```
    pnpm install --frozen-lockfile
    pnpm build
    ```

    The expected outcome:
    - `pnpm install` succeeds (lockfile from Task 1 satisfies all transitive deps).
    - `pnpm build` runs `vue-tsc && vite build`. Both must exit 0.
    - The build emits files under `internal/api/web_dist/` (per vite.config.ts).

    If `vue-tsc` reports errors about missing `traffic.*` keys, the keys from Task 2 weren't added — fix Task 2, not the typecheck. If `vite build` complains about uPlot, the install in Task 1 didn't take — re-run.

    Do NOT commit the `web_dist/` output — `.gitignore` already excludes it. Do NOT change the vite config.
  </action>
  <verify>
    <automated>cd /home/david/workspace/mitigador/web &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web install --frozen-lockfile` exits 0
    - `pnpm --dir web build` exits 0
    - `test -d /home/david/workspace/mitigador/internal/api/web_dist`
    - `test -f /home/david/workspace/mitigador/internal/api/web_dist/index.html`
    - Build output contains at least one JS chunk that references uplot (sanity check that uPlot is bundled even though not yet imported — actually it WON'T be referenced yet since no code imports it; this is fine. The acceptance is just that the build succeeds.)
    - No new files in `web/` are untracked except the lockfile delta from Task 1 (verify with `git status web/`)
  </acceptance_criteria>
  <done>
    `pnpm build` exits 0. The placeholder /traffic route is included in the SPA. uPlot is installed but not yet imported (Plan 04 imports it).
  </done>
</task>

</tasks>

<verification>
- `pnpm --dir web typecheck` exits 0
- `pnpm --dir web build` exits 0
- Both locale files parse as JSON (the Task 2 verify command)
- All `acceptance_criteria` grep checks pass
- Manual smoke (optional, not blocking): start the Go backend (`go run ./cmd/mitigador serve`) + `pnpm dev` and click "Tráfego" in the sidebar — the empty placeholder must render in pt-BR by default and toggle to en-US via the locale switcher.
</verification>

<success_criteria>
- `uplot` is a runtime dependency (`web/package.json` + `web/pnpm-lock.yaml`).
- The frontend has a `/traffic` route resolving to a placeholder `TrafficView` rendered inside `AppLayout` with a translated title.
- The sidebar shows "Tráfego" / "Traffic" with proper active-class highlighting.
- Both locale files contain the full `traffic.*` key set used by everything Plan 04 and Plan 05 will add — no surprise keys later.
- `web/src/api/traffic.ts` exists with typed wrappers for the two Plan 03 endpoints, ready to be imported by Plan 05.
- `pnpm build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-flow-analytics-top-talkers-per-host-charts/01.1-02-SUMMARY.md` documenting:
- Lockfile delta (uplot pinned version)
- Full list of i18n keys added (verbatim copy of both locale blocks)
- Route + sidebar wiring
- API client surface (signatures + JSON shape expected from Plan 03)
- Followups: Plan 04 will import `uplot` and `uplot/dist/uPlot.min.css`; Plan 05 will replace the placeholder view body.
</output>
